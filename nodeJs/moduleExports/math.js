const sum = (a, b) => a+b;
const product = (a,b) => a*b;
const pi = 3.14
const square = (a) => a*a;

let obj = {
    sum : sum,
    product : product,
    pi : pi,
    square : square
}

module.exports = obj;